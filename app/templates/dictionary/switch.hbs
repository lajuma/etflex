{{#if session.isAuthenticated}}
  {{outlet}}
  <div class="logout">
    <a href='#' class="logout-link" {{action 'logout'}}>Logout</a>
  </div>
{{else}}
  {{etf-login-form username=username password=password clickAction=(action 'login' username password)}}
{{/if}}
